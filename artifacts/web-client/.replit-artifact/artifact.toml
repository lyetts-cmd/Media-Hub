kind = "web"
previewPath = "/"
title = "Cadence Music"
version = "1.0.0"
id = "artifacts/web-client"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21320

[services.development]
run = "pnpm --filter @workspace/web-client run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/web-client", "run", "build" ]
publicDir = "artifacts/web-client/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21320"
BASE_PATH = "/"
